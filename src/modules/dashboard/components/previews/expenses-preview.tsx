import type { WidgetPreviewProps } from '../widget-card';

export function ExpensesPreview(_props: WidgetPreviewProps) {
    return (
        <p className="line-clamp-3 text-xs text-muted-foreground">
            Expenses preview (TODO)
        </p>
    );
}
