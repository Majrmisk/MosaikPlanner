import type { WidgetPreviewProps } from '../widget-card';

export function CalendarPreview(_props: WidgetPreviewProps) {
    return <p className="line-clamp-3 text-xs text-muted-foreground">Calendar preview (TODO)</p>;
}
