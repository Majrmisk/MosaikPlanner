import type { WidgetPreviewProps } from '../widget-card';

export function SpinnerPreview(_props: WidgetPreviewProps) {
    return <p className="line-clamp-3 text-xs text-muted-foreground">Spinner preview (TODO)</p>;
}
